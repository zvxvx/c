#ifndef UTILS_H
#define UTILS_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * Strips the carriage return from the String passed in
 * @param array Representing the array to strip the \r\n from
 */
void strip(char * array);


#endif
